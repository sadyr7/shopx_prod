# from django.contrib import admin
# from .models import SupportServiceRoom, SupportServiceMessage

# class SupportRoomAdmin(admin.ModelAdmin):
#     list_display = ['name','slug','id']

# class SupportMessageAdmin(admin.ModelAdmin):
#     list_display = ['support_room','user','id']

# admin.site.register(SupportServiceRoom,SupportRoomAdmin)
# admin.site.register(SupportServiceMessage,SupportMessageAdmin)
